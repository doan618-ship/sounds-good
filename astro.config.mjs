import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Configure site URL for custom domain or GitHub Pages deployment
  site: 'https://musicreviews.example.com',
  output: 'static',
});
